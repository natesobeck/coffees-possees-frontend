import * as data from '../../../public/assets/data.js'

const NewClub = () => {
  return ( 
    <main>
      <h1>{data.club.name}</h1>
      <form>
      </form>
    </main>
  )
}

export default NewClub